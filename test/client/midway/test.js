'use strict';

describe('A test suite for midway', function() {
  beforeEach(function() {
  });
  afterEach(function() {
  });
 
  it('should succeed', function() {
    expect(true).to.be.true;
  });
});
