'use strict';

describe('A test suite for unit', function() {
  beforeEach(function() {
  });
  afterEach(function() {
  });
 
  it('should succeed', function() {
    expect(true).to.be.true;
  });
});
